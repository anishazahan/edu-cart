/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["example.com", "cdn.pixabay.com"], // Add 'example.com' or any other domains you need
  },
};

export default nextConfig;
